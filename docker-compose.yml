services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - tcoffline-data:/data
    environment:
      - DATABASE_URL=sqlite:////data/local.db
      - ENVIRONMENT=production
    restart: unless-stopped

volumes:
  tcoffline-data:
